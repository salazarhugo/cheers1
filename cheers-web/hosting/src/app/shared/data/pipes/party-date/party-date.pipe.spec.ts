import { PartyDatePipe } from './party-date.pipe';

describe('PartyDatePipe', () => {
  it('create an instance', () => {
    const pipe = new PartyDatePipe();
    expect(pipe).toBeTruthy();
  });
});
